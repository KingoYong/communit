<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<!-- 根标签 -->
<configuration>
	<properties>
		<property name="driver" value="com.mysql.jdbc.Driver"/>
		<property name="url" value="jdbc:mysql://192.168.0.231:3306/mybatis-110?useUnicode=true&amp;characterEncoding=utf-8&amp;allowMultiQueries=true"/>
		<property name="username" value="ma"/>
		<property name="password" value="123456"/>
	</properties>

	<settings>
		<!--将数据库里的下划线连接自动和实体中属性的驼峰式匹配，如userName和user_name -->
		<setting name="mapUnderscoreToCamelCase" value="true"/>
		<!--开启二级缓存,全局总开关，这里关闭，mapper中开启了也没用-->
		<!--<setting name="cacheEnabled" value="false"/>-->
	</settings>

	<!--类型别名是为 Java 类型命名的一个短的名字。它只和 XML 配置有关，存在的意义仅在于用来减少类完全限定名的冗余-->
	<typeAliases>
		<!--type:实体类的全路径。alias:别名，通常首字母大写。缺点是要为每个每个实体都要配置一个，可以使用下面的package解决-->
		<!--<typeAlias type="com.zpc.mybatis.pojo.User" alias="User"/>-->
<!--		<package name="com.example.demo.util.dto"/>-->
	</typeAliases>

	<!-- 环境，可以配置多个，default：指定采用哪个环境 -->
	<environments default="test">
		<!-- id：唯一标识 -->
		<environment id="test">
			<!-- 事务管理器，JDBC类型的事务管理器 -->
			<transactionManager type="JDBC" />
			<!-- 数据源，池类型的数据源 -->
			<dataSource type="POOLED">
				<property name="driver" value="com.mysql.jdbc.Driver" />
				<property name="url" value="jdbc:mysql://192.168.0.231:3306/ssmsemo" />
				<property name="username" value="ma" />
				<property name="password" value="123456" />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<!--直接映射到相应的mapper文件 -->
		<mapper resource="mappers/ResponseAppIdMapper.xml" />
		<mapper resource="mappers/ResponseMapper.xml"/>
	</mappers>

</configuration>

